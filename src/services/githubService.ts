export const setPublicOrderToken = (_encodedToken: string) => {};

export const fetchDb = async () => ({ products: [], orders: [], categories: [] });
export const updateDb = async () => ({});
export const validateToken = async () => false;